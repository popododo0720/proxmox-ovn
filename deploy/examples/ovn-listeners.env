# Install unchanged as /etc/pvn/central/ovn-listeners.env on every voter.
# OVN's Connection rows are replicated, so secure passive listeners bind all
# local addresses. Restrict these ports with the host firewall.
PVN_OVN_NB_PORT=6641
PVN_OVN_SB_PORT=6642
